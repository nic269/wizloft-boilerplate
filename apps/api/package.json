{
  "name": "@repo/api-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx watch src/index.ts",
    "build": "tsup",
    "start": "node dist/index.cjs",
    "check-types": "tsc --noEmit",
    "test": "echo 'api app tests live in @repo/api'",
    "test:unit": "echo 'api app unit tests live in @repo/api'",
    "test:integration": "echo 'api app integration tests live in @repo/api'"
  },
  "dependencies": {
    "@hono/node-server": "^2.0.8",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "@repo/api": "workspace:*",
    "@repo/auth": "workspace:*",
    "@repo/database": "workspace:*",
    "@t3-oss/env-core": "^0.13.11",
    "hono": "^4.12.27",
    "pg": "^8.22.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/node": "^26.1.0",
    "tsup": "^8.5.1",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3"
  }
}
